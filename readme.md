> 可能用得到的一些命令

```shell
python3 -m venv env	#创建虚拟环境
source env/bin/activate  # Linux是这个写法，如果是windows请使用`env\Scripts\activate`
pip install -r requirements.txt #安装环境
python3 manage.py makemigrations
python3 manage.py migrate
```



